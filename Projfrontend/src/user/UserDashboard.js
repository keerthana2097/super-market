import React from "react"
import Home from "../core/Home";

const UserDashboard = () => {
    return (
        <Home title="User dashboard">
            <h1>Welcome to User dashboard</h1>
        </Home>
    );
};

export default UserDashboard;